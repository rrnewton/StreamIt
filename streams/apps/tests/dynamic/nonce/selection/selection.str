void->void pipeline selection {
    add FileReader<float>("../inputs/floats.in");
    add select();
    add FileWriter<float>(stdout);
}

float->float filter select() {
    work pop 1 push * {
        float x = pop();
        if (x < 5.0) {
            push(x);
        }
    }
}

