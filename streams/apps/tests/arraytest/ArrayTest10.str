/**
 * This tests resolving constant expressions within dimensions of
 * parameters.
 **/

void->void pipeline ArrayTest10 {
    add Source();
    add MotionPrediction(10);
    add Printer();
}

int->int filter MotionPrediction(int N) {
    work pop 1 push 1 {
	push(pop());
    }
    
    void readBPicture() {
	int[N/2] vector;
	predictMotionPixel(vector);
    }

    void predictMotionPixel(int[N/2] foo) {}
}

void->int stateful filter Source {
    int i = 0;
    work push 1 { push(i++); }
}

int->void filter Printer {
    work pop 1 { println(pop()); }
}
